# Build artifacts
build*/

# Local backups from the macOS -> headless Linux port
*.orig

# Vendored/downloaded build-env deps (utf8cpp); not referenced by CMakeLists
deps/
__pycache__/
*.pyc

# Stray MinGW preprocessor output accidentally generated from .vscode/*.json
.vscode/*.exe

# Job search agent: personal data stays local (job-search-agent/SPEC.md §12)
job-search-agent/profile.local.md
job-search-agent/queue/
job-search-agent/tracker.csv
